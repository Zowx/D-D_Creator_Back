export interface Ability {
    id: bigint;
    name: string;
    description: string;
    shortDescription: string;
}

export interface AbilityCandidate {
    name: string;
    description: string;
    shortDescription: string;
}